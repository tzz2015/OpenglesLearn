<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.opengleslearn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SecondActivity" />
        <activity android:name=".activity.FirstActivity" />
        <activity android:name=".activity.ThreeActivity" />
        <activity android:name=".activity.FourActivity" />
        <activity android:name=".activity.FiveActivity" />
        <activity android:name=".activity.SixActivity" />
        <activity android:name=".activity.SevenActivity" />
        <activity android:name=".activity.ParticleActivity" />
        <activity android:name=".activity.PictureLoadActivity" />
        <activity android:name=".activity.AnimationIntActivity"/>
        <activity android:name=".activity.AnimationOutActivity"/>

    </application>

</manifest>